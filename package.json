{
  "name": "keep-sim",
  "version": "1.0.0",
  "description": "A minimal PWA to keep your SIM card active by consuming ~100KB of mobile data.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "generate:payload": "node scripts/generate-payload.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vite-plugin-pwa": "^0.19.0"
  }
}
